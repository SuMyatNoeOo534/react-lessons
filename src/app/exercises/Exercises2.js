"use client";
import React from "react";
export default function Person(){
    return(
        <div>
        <h1>Function Component</h1>
        <p>Function components are the modern,simpler way to write React components.They're JavaSccript functionsthat return JSX and with addition of Hooks,they can now handle state,lifecycle and more.</p>
        </div>
    );
}